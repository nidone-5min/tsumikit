# AGENTS.md

## プロジェクト概要

- `tsumikit` は、YouTube LiveおよびTwitchのイベントを受け取り、OBS Browser Sourceで演出を実行するデスクトップアプリです。
- 現在はPhase 0の技術検証段階です。
- デスクトップシェルとバックエンドにはWails v2とGo、フロントエンドにはReact、TypeScript、Vite、Tailwind CSSを使用します。
- Windowsを開発およびQAの優先環境とします。可能な範囲でmacOSでの開発も維持してください。

## 配布・利用の前提

- 本アプリは不特定の利用者への配布を想定したデスクトップアプリです。開発者の管理下にある単一環境だけで動作することを前提にしないでください。
- 複数の利用者が、それぞれの端末・配信アカウント・OBS環境で同時に利用する可能性があります。
- 利用者ごとの認証情報、設定、イベント、生成データは明確に分離し、別の利用者や別のアカウントへ漏えい・混在しない設計にしてください。
- 外部サービスのAPI制限や障害が全利用者へ連鎖しないよう、利用者または接続単位で状態、再試行、レート制限を扱ってください。
- 開発環境にだけ存在するファイル、環境変数、固定パス、認証状態に依存しないでください。

## リポジトリ構成

- Wailsの起動処理とアプリケーションのバインディングは、リポジトリ直下のGoファイルに配置してください。
- フロントエンドのアプリケーションコードは `frontend/src/` 配下に配置してください。
- `frontend/wailsjs/` はWailsが生成するバインディングとして扱ってください。生成ファイルを直接編集せず、バインディングを変更した場合はWailsのワークフローを通して再生成してください。
- `frontend/dist/` と `build/bin/` は生成されるビルド成果物として扱い、コミットしないでください。

## 開発ルール

- タスクで明示的に変更を求められていない限り、既存の設計と命名規則を維持してください。
- 変更は依頼された動作に集中させ、無関係なリファクタリングを避けてください。
- `README.md` の記載に従い、Go `1.25.0` 以上、Node.js `24`、npm、Wails `v2.15.0` を使用してください。
- フロントエンドの依存関係は `npm ci` で復元し、依存関係を変更した場合は `frontend/package-lock.json` も同期してください。
- 本番用の依存関係を追加または置換する場合は、その必要性を説明してください。
- 認証情報、トークン、`.env` ファイルなどの秘密情報をコミットしないでください。設定例が必要な場合は、機密情報を除いた内容を `.env.example` に追加してください。
- セットアップ手順、対応環境、コマンド、またはユーザーから見える動作を変更した場合は、`README.md` も更新してください。

## イベント処理と外部接続

- イベント処理では、同時実行、重複受信、順序の入れ替わりが発生する前提で設計してください。
- 重複実行によって利用者への演出や状態更新が二重に行われないよう、必要な処理には冪等性を持たせてください。
- 複数のgoroutineや非同期処理から状態を更新する場合は、競合状態が発生しないことを確認してください。
- 再接続時は既存の接続と処理を適切に終了し、接続やイベント購読が重複しないようにしてください。
- APIのレート制限と `Retry-After` を尊重し、再試行には上限付きの指数バックオフとジッターを使用してください。
- キャンセルやアプリ終了時には、goroutine、WebSocket、HTTP接続、タイマーなどのリソースを確実に解放してください。
- 関連する実装には、重複イベント、順序の逆転、切断と再接続、キャンセルを扱うテストを追加してください。

## 認証と秘密情報

- デスクトップアプリのバイナリやフロントエンドコードに、クライアントシークレット、APIキー、固定トークンを埋め込まないでください。
- デスクトップアプリをOAuthの公開クライアントとして扱い、認証にはAuthorization Code Flow with PKCEとOSの外部ブラウザを使用してください。Implicit Flowや埋め込みWebViewで認証しないでください。
- 利用者のアクセストークンや更新トークンは、OSの安全な資格情報ストアを使用して保存してください。安全な保存先を利用できない場合は平文で保存せず、セッション中だけ保持するか保存機能を無効化してください。
- 認証フローでは、state値、PKCE verifier、リダイレクト先、認証コードの一回限りの使用、トークンの有効期限と更新処理を検証してください。
- OAuthコールバック用のローカルHTTPサーバーを使用する場合はループバックアドレスだけで待ち受け、認証完了またはタイムアウト時に停止してください。
- 認証情報をログ、画面、エラーメッセージへ出力しないでください。

## データ保存と互換性

- 設定や永続化データの形式を変更する場合は、既存バージョンからの移行方法と後方互換性を検討してください。
- データ保存は、異常終了や書き込み途中でもファイルが破損しにくい方法で行ってください。
- 読み込みに失敗した場合は、安全な初期値へ戻し、利用者の既存データを不用意に削除しないでください。
- 保存データにスキーマまたはバージョンを持たせ、将来の移行を可能にしてください。

## ログとプライバシー

- ログには認証情報、個人情報、配信コメントの全文などを不用意に記録しないでください。
- 利用者がログをサポートへ共有する可能性を前提に、機密情報をマスクしてください。
- テレメトリやクラッシュレポートを導入する場合は、収集内容を明示し、必要に応じて利用者の同意と無効化手段を用意してください。

## 配布と更新

- 配布物に開発用設定、ソースマップ、テスト用認証情報、不要なデバッグ機能を含めないでください。
- 自動更新やファイルのダウンロードを実装する場合は、配布元、署名、ハッシュなどを検証してください。
- インストールや実行に不要な管理者権限を要求しないでください。
- 依存関係とライセンスが配布条件に適合していることを確認してください。

## 検証

- フロントエンドを変更した場合は、`frontend/` で次を実行してください。
  - `npm run check`
  - `npm run build`
- Goを変更した場合は、次を実行してください。
  - 変更したGoファイルを `gofmt` で整形してください。
  - リポジトリ直下で `go test ./...` を実行してください。
- goroutine、共有状態、非同期イベント処理などの並行処理を変更した場合は、対応環境で `go test -race ./...` を実行してください。
- Goの依存関係を追加または更新した場合は、ツールとネットワークを利用できる環境で `govulncheck ./...` を実行してください。
- フロントエンドの依存関係を追加または更新した場合は、`frontend/` で `npm audit --audit-level=high` を実行してください。
- 依存関係の脆弱性検査は、継続的に実行できるようCIへ組み込んでください。検出結果を無効化する場合は、影響、理由、期限を記録してください。
- Wailsの設定、バインディング、アセット、またはパッケージングに影響する変更では、ローカル環境が対応している場合に `wails build -clean` を実行してください。
- 動作を変更した場合は、テストを追加または更新してください。変更箇所に近い、対象を絞ったテストを優先してください。
- 実行できなかった検証コマンドがある場合は、コマンドと実行できなかった理由を報告してください。

## 実装・レビューフロー

- 実装タスクでは、実装後に次の2回のレビューと修正を行ってください。
- 2回のレビューは、いずれもモデルに `gpt-6-astra` を指定したサブエージェントへ依頼してください。実装を担当したエージェント自身の確認だけで代替しないでください。
  1. 1回目のレビューでは、依頼内容との一致、機能上の正しさ、境界値、エラー処理、テスト不足、既存機能への影響を確認してください。
  2. 1回目のレビューで見つかった問題を修正し、関連するテストを実行してください。指摘がない場合、この修正工程はスキップできます。
  3. 修正後の差分全体を改めてレビューしてください。2回目は、セキュリティ、脆弱性、認証情報、利用者ごとのデータ分離、並行処理、再接続、リソース解放、配布環境での回帰を重点的に確認してください。
  4. 2回目のレビューで見つかった問題を修正し、関連するテストを再実行してください。指摘がない場合、この修正工程はスキップできます。
- 2回目の修正後に最終差分を確認し、「検証」セクションに該当するコマンドを実行してください。
- 各レビューで確認した観点、見つかった問題、実施した修正、未解決事項を完了報告に含めてください。
- 未解決の重大な問題がある状態で、タスクを完了として扱わないでください。

## Code Review Rules

- 機能上の正しさだけでなく、セキュリティ、脆弱性、プライバシー、複数利用者への配布、並行処理への影響を確認してください。
- 指摘は、影響範囲と発生条件が分かるように具体的に記述し、可能であれば安全な修正方法も示してください。
- 次の項目を重点的に確認してください。
  - YouTube Live、Twitch、OBS、Wailsのフロントエンドなど、信頼境界を越えて入る値を未検証のまま使用していないか。
  - 外部由来の文字列を画面やBrowser Sourceへ表示する際に、XSSやHTML・スクリプト注入が発生しないか。
  - URL、ファイルパス、コマンド、IPC、ローカルHTTP通信の扱いに、SSRF、パストラバーサル、コマンド注入、意図しない外部公開がないか。
  - ローカルサーバーを使用する場合、必要なく全インターフェースへ公開していないか。認証・認可が必要な操作を第三者が実行できないか。
  - OAuthトークン、APIキー、Cookie、個人情報を平文で不用意に保存・表示・ログ出力・エラー送信していないか。権限スコープが必要最小限か。
  - 利用者、配信アカウント、接続セッションごとの状態が分離され、キャッシュ、グローバル変数、永続化データを介して混在しないか。
  - 同時イベント、再接続、重複配信、処理順序の逆転によって、競合状態、データ破損、演出の二重実行が発生しないか。
  - APIのレート制限、タイムアウト、一時障害に対して、無制限の再試行や一斉再接続を行わず、バックオフとキャンセルを適切に扱っているか。
  - goroutine、WebSocket、HTTP接続、タイマー、ファイルなどが終了・切断時に解放され、長時間利用でリークしないか。
  - 依存パッケージの追加や更新に既知の脆弱性、不要な権限、保守性の問題がなく、ロックファイルが正しく更新されているか。
  - 配布物に秘密情報、デバッグ設定、開発用エンドポイント、不要なファイルが含まれていないか。
  - エラーメッセージは利用者が対応できる内容になっており、同時に機密情報や内部実装を過剰に露出していないか。
- 見た目や好みだけの指摘より、実際に不具合、漏えい、権限逸脱、データ混在、可用性低下につながる問題を優先してください。

## 完了条件

- 関連するフォーマット、lint、型チェック、テスト、ビルドが成功していること。
- 生成ファイルや無関係なファイルが意図せず変更に含まれていないこと。
- ドキュメントが実際の動作および開発手順と一致していること。
